#include<bits/stdc++h>
const int maxn=1e8+7;
int a[maxn];
using namespace std;
int main()
{
    int n,m;
    int sum=0;
    while(cin>>n>>m)
    {
       for(int i=1;i<=n;i++)
       {
           sum+=i;
           if(sum==m)
       }
    }
    return 0;
}
